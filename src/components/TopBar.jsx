import React from 'react';

const TopBar = () => {
    return (
        <div className="bg-brand-cyan text-brand-black py-2 px-4 text-center font-bold text-sm md:text-base flex justify-center items-center gap-4">
            <span className="font-mono text-lg uppercase tracking-wider">futboltshop</span>
        </div>
    );
};

export default TopBar;
